import styled from 'styled-components';

const StyledCommunityPage = styled.div`
	/* border: 1px solid red; */
	padding-top: 8.5rem;
	@media (max-width: ${(pr) => pr.theme.tablet}) {
	}
`;

export default StyledCommunityPage;
